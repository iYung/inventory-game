# Food Cart Scene Art Checklist

- [x] Task A — `scripts/gen_scene_art.py`, `assets/images/scene/`, `assets/images/customer.png`, `assets/images/merchant.png` — Write and run a Python (PIL) script that pixel-paints all scene assets: sky layer (640×280 px, muted blue + white cloud rectangles), horizon silhouette (640×60 px, dark building/tree shapes against the sky), street layer (640×80 px, tan cobblestone grid), window frame (1280×80 px, two thick brown vertical wood pillars left/right + wide horizontal sill across bottom), customer sprite (~48×72 px Oregon Trail-style figure: dark hat, earthy torso, dark pants + legs), and merchant sprite (same dimensions, different hat + color). Save layers to `assets/images/scene/sky.png`, `horizon.png`, `street.png`, `window_frame.png`; sprites to `assets/images/customer.png` and `assets/images/merchant.png`. Run the script and verify all files exist before marking done.

- [x] Task B — `game/scenes/kitchen_scene.lua` — Replace the flat `stage_bg` color-fill in `KitchenScene:draw()` with four layered image draws (sky at y=0, horizon at y≈220, street at y≈280, window_frame at y≈280) using `love.graphics.draw`. Load the four scene images in `KitchenScene:on_enter()` and store them on `self`. Keep all existing HUD, panel, and customer draw calls unchanged; only the background painting changes.

- [x] Task C — `lua/game/customer.lua` — Adjust the customer sprite to render at 2× scale so the new 48×72 px PNG reads as ~96×144 on screen. Update `CW` and `CH` constants to match (CW=96, CH=144), or pass `sx=2, sy=2` to `Sprite:draw()` — whichever fits the existing Sprite API. Verify the customer still walks in, centers on screen, and the speech bubble tail still points to them correctly.
