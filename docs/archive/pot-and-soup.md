## Pot and Soup Checklist

- [x] Task A — `lua/game/data/item_defs.lua` — rename type key `dutch_oven` → `pot`; change display name `"Dutch Oven"` → `"Pot"`; change the microwave's container recipe from `{container="dutch_oven", requires={potato=1,water=1,raw_meat=1}, produces={beef_stew=1}}` to `{container="pot", requires={water=1,raw_meat=1}, produces={soup=1}}`; add a `soup` item def `{name="Soup", footprint={{0,0}}, color={0.70,0.50,0.30,1}, tags={"Protein","Hearty"}}`
- [x] Task B — `game/scenes/kitchen_scene.lua` — rename every `dutch_oven` identifier (local variable name and `Item.new("dutch_oven")` string) to `pot`/`"pot"`; update the comment on line 84 from "dutch oven" to "pot"
- [x] Task C — `tests/test_item.lua` — update all `dutch_oven` variable names, `"dutch_oven"` type strings, and comment text (Tests 6, 7, 8) to use `pot`; update the beef_stew expectation in Test 6 to expect `soup` in the pot's panel after cooking, and adjust the loaded ingredients to `water` + `raw_meat` (drop `potato`); update Test 7 to use `water` only (drop `potato`), reflecting the new two-ingredient recipe; update Test 8's "no container item present" assertion strings
- [x] Task D — `tests/test_item_panel.lua` — update all `dutch_oven` variable names, `"dutch_oven"` type strings, and comments (Tests 15, 16, 17) to use `pot`; in Tests 15–17 drop `potato` from the pot's panel (the new recipe only needs `water` + `raw_meat`); adjust any assertion messages accordingly
- [x] Task E — `tests/test_kitchen_scene.lua` — update all `dutch_oven` variable names, `"dutch_oven"` type strings, and comment text (Tests 17, 17b) to use `pot`
