## Veggie Label Checklist

- [x] Task A — `lua/game/data/item_defs.lua` — add `"Veggie"` to `steamed_broccoli`'s tags: `{ "Healthy" }` → `{ "Healthy", "Veggie" }`
- [x] Task B — `lua/game/data/item_defs.lua` — add `"Veggie"` to `onion_soup`'s tags: `{ "Hearty" }` → `{ "Hearty", "Veggie" }`
