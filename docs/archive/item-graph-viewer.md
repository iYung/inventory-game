## Item Graph Viewer Checklist

- [x] Task A — `scripts/item_graph.html` — Create the self-contained HTML file with all item/recipe data as a JS object (mirroring item_defs.lua), vis-network graph, node coloring by category, edge arrows (solid for instant, dashed for overnight/garden), sidebar tag toggles, hover tooltips, and fix-positions toggle.
