## Customer Order Panel Checklist

- [x] Task 1 — `lua/game/config.lua` — Add `config.ORDER_PANEL_COLS = 3` and `config.ORDER_PANEL_ROWS = 3`, placed next to the existing `config.MERCHANT_PANEL_COLS` / `config.MERCHANT_PANEL_ROWS` block (currently lines 16-17). These size the new order-customer panel Grid, mirroring how the merchant panel constants already size the merchant stock Grid. No other changes to this file. Independent — no dependencies.

- [x] Task 2 — `lua/game/data/item_defs.lua` — Add a new non-placeable entry `order_customer = { name = "Customer" }`, placed next to the existing `merchant = { name = "Merchant" }` entry (currently lines 62-65) and following the same comment convention ("Never placed via Item.new/on a Grid — used only by ItemPanel's title bar / def lookup for an order-kind Customer"). This gives `ItemPanel.new` something to look up via `item_defs[item.type_id]` for the order panel's title bar text ("Customer"), the same way the merchant panel gets "Merchant". No other changes to this file. Independent — no dependencies.

- [x] Task 3 — `lua/game/item.lua` — Add a public `Item.has_tag(item, tag)` helper: a plain function (not a method needing `:`) that returns `true` iff `tag` appears in `item.tags`, `false` otherwise. Implement it as a local function near the other helpers (e.g. next to `find_action`/`matching_recipes`) with the same logic already duplicated as the local `has_tag` in `game/scenes/kitchen_scene.lua` (currently lines 148-153: loop `for _, t in ipairs(item.tags) do if t == tag then return true end end return false`), then export it at the bottom of the file alongside the existing `Item.matching_recipes = matching_recipes` line (currently line 318), i.e. add `Item.has_tag = has_tag`. This will be consumed by `item_panel.lua`'s new Serve-enablement check (Task 5). Independent — no dependencies.

- [x] Task 4 — `lua/game/customer.lua` — In `Customer:show(cfg)` (currently lines 96-132), extend the `if self.kind == "merchant" then ... end` block (lines 101-109) with an `elseif self.kind == "order" then` branch that mirrors the merchant case but for order customers: set `self.type_id = "order_customer"` and `self.panel = Grid.new(config.ORDER_PANEL_COLS, config.ORDER_PANEL_ROWS, config.U, 0, 0)`. Do not seed any starting contents (unlike the merchant branch's `place_first_fit` stock loop) — the order panel always starts empty; the player drags items into it. Since `self.kind = cfg.kind or "order"` (line 99) makes `"order"` the default kind, this means every ordinary food-order customer now gets a 3x3 `panel` Grid and a `type_id` of `"order_customer"`, the same way merchant customers already get theirs. No other changes to this file. Depends on Task 1 (`config.ORDER_PANEL_COLS`/`ORDER_PANEL_ROWS` must exist).

- [x] Task 5 — `lua/game/item_panel.lua` — Add order-kind panel support, mirroring the existing merchant-kind special-casing (`item.kind == "merchant"`) throughout this file:
  - **Reminder text row**: When `self.item.kind == "order"`, the panel needs an extra row of vertical space between the title bar and the grid showing reminder text (e.g. `"Order: " .. self.item.requested_tag`, since `self.item` is the `Customer` and `Customer` already has a `requested_tag` field — see `lua/game/customer.lua`). Add a new layout tuning constant near the top (alongside `TITLE_H` etc.), e.g. `local REMINDER_H = 24`. In `ItemPanel.new`'s `bg_h` calculation (currently `self.bg_h = TITLE_H + MARGIN + self.grid_h + BUTTON_GAP + BUTTON_H + MARGIN`, line 84) and in `ItemPanel:_layout`'s `self.grid_y` calculation (currently `bg_y + TITLE_H + MARGIN`, line 112), add `REMINDER_H` into the vertical stack only when `self.item.kind == "order"` (store `self.item.kind == "order"` in `_layout`/`new` as needed, or just check inline both places). Draw the reminder text in `ItemPanel:draw`, between drawing the title bar and calling `self.item.panel:draw(...)`, only when `self.item.kind == "order"`.
  - **Serve / Skip buttons**: In `ItemPanel.new`'s button-count/sizing logic (currently `local button_count = #actions + ((item.kind == "merchant") and 1 or 0)`, line 77), change so that `item.kind == "order"` adds 2 to `button_count` instead of 1 (Serve + Skip), while `item.kind == "merchant"` still adds 1 (Leave) as today — these are mutually exclusive kinds so a simple `elseif` on `item.kind` is fine. In `ItemPanel:_layout`'s button-slot loop (currently the `if self.item.kind == "merchant" then ... self.buttons["Leave"] = ... end` block, lines 134-138), add a parallel `elseif self.item.kind == "order" then` branch that assigns `self.buttons["Serve"]` and `self.buttons["Skip"]` to the next two slots in the same row, same sizing (`BUTTON_W`/`BUTTON_H`) as every other button.
  - **Serve enablement**: Add a private helper (e.g. `ItemPanel:_serve_enabled()`) that returns `true` only when `self.item.kind == "order"` and `self.item.panel:items()` has exactly one entry and `Item.has_tag(self.item.panel:items()[1], self.item.requested_tag)` is true (use the `Item.has_tag` helper added in Task 3 — `Item` is already required at the top of this file). Skip has no enablement check — it is always clickable, same treatment as the merchant's "Leave".
  - **Input handling**: In `ItemPanel:mouse_pressed` (currently lines 202-241), add handling for the new buttons, inserted in the same place the existing `self.buttons["Leave"]` check lives (lines 217-221): if the click lands on `self.buttons["Serve"]` AND `self:_serve_enabled()` is true, set `self.should_close = true` and a new `self.should_serve = true` flag (initialize `self.should_serve = false` and `self.should_skip = false` in `ItemPanel.new` next to `self.should_leave = false`), then `return true`. If the click lands on `self.buttons["Serve"]` but it's *not* enabled, still `return true` (click absorbed, no-op — matching how a disabled action button behaves in the generic `_button_at` handling below it). If the click lands on `self.buttons["Skip"]`, set `self.should_close = true` and a new `self.should_skip = true` flag, then `return true`. `ItemPanel` itself does **not** touch the panel's items, does **not** call any `Customer` method, and does **not** touch `day_state` — exactly like the existing "Leave" button, it only raises flags for the caller (`KitchenScene`, Task 6) to act on.
  - **Draw**: In `ItemPanel:draw` (currently lines 270-333), draw "Serve" and "Skip" the same way "Leave" is drawn today (lines 318-324) — solid rect + label — except Serve uses the disabled/enabled color split like the action buttons above it (`self:_serve_enabled()` picks `colors.button` vs `COLOR_DISABLED`, same pattern as `is_action_enabled` does for `action.name` at lines 295-296), while Skip is always drawn in its normal enabled color (reuse `colors.button`, not `COLOR_LEAVE`/red, since Skip isn't a warning action the way Leave/close are).
  Depends on Task 2 (`order_customer` def for the title bar lookup), Task 3 (`Item.has_tag`), and Task 4 (`Customer:show` must actually populate `panel`/`type_id`/`requested_tag`/`kind` for order customers before this is testable end-to-end, though the code changes here can be written against the `Customer` shape as documented).

- [x] Task 6 — `game/scenes/kitchen_scene.lua` — Three changes:
  1. **Click-to-open the order panel**: In `KitchenScene:mouse_pressed`, inside the `if self.customer:active() and self:_customer_hit(x, y) then` block (currently lines 418-433), add a new branch — parallel to the existing `if self.customer.kind == "merchant" and self.customer:arrived() then self:_open_or_focus_panel(self.customer) return end` (lines 423-426) — that reads `if self.customer.kind == "order" and self.customer:arrived() and self.customer.done_talking then self:_open_or_focus_panel(self.customer) return end`. Place it right after the merchant branch, before the existing `advance_after`/`advance` fallback. This preserves today's behavior exactly while the greeting is still typewriter-revealing (`done_talking == false`: falls through to `self.customer:advance()`, which still just reveals/advances the message), and opens/focuses the order panel once the greeting has finished revealing (a click that previously no-op'd, since `advance()` returns immediately when `done_talking` is true).
  2. **Delete the drag-onto-customer block**: In `KitchenScene:mouse_released`, delete the entire `if self.customer:arrived() and self.customer.kind ~= "merchant" and self:_customer_hit(x, y) then ... end` block (currently lines 532-547, including its `clear_drag(owner, item)`, the `has_tag(item, self.customer.requested_tag)` branch, and both `serve()`/`dismiss("Sorry, that's not what I ordered!")` calls). After deletion, a drop on the customer's sprite body falls straight through to the existing `local hover = self:_hover_grid(x, y)` logic below (currently line 549 onward) exactly like a drop on any other non-grid point on screen: `_hover_grid` returns `self.grid` by default (the customer isn't a grid), so the item resolves via the normal transfer/place/snap-back path — i.e. it snaps back to where it was picked up, since the customer's on-screen position is not a valid grid cell. Also delete the now-dead local `has_tag` helper function (currently lines 147-153, right above `KitchenScene:_customer_hit`) — its only caller was the block just deleted; `Item.has_tag` (Task 3) is the shared replacement, used from `item_panel.lua` (Task 5) instead.
  3. **Wire Serve/Skip into day_state and the customer**: In `KitchenScene:mouse_pressed`'s panel-hit loop (currently lines 382-402), alongside the existing `if panel.should_leave then self.customer:dismiss() self.day_state:record_dismiss() end` check, add:
     - `if panel.should_serve then ... end`: take the single item out of the panel's grid — `local served_item = panel.item.panel:items()[1]` then `panel.item.panel:remove(served_item)` (mirror the removal style `clear_drag`/`remove_matching` already use elsewhere in this codebase — snapshot before removing if iterating, though here there's only ever exactly one item since `ItemPanel:_serve_enabled()` already guaranteed that before setting the flag) — then call `self.customer:serve()` and `self.day_state:record_serve()`.
     - `if panel.should_skip then ... end`: return every item currently in the panel's grid to the main floor grid, first-fit each (snapshot `panel.item.panel:items()` into a plain list first, since removing mutates the list the grid returns — same caution `lua/game/item.lua`'s `remove_matching` already takes), removing each from the panel grid and placing it via `self.grid:place_first_fit(item)` (already used elsewhere in this file, e.g. `transfer_drag_first_fit`, so no new Grid API is needed). Then call `self.customer:dismiss(<a new distinct message>)` — add a new local string constant near the top of the file (alongside `DOUBLE_CLICK_WINDOW`), e.g. `local SKIP_MESSAGE = "Maybe next time!"`, distinct from the existing `"Sorry, that's not what I ordered!"` wrong-item message (which is now unreachable, since Serve stays disabled unless the grid holds exactly one matching item) — and `self.day_state:record_dismiss()`.
     - Both new checks go before the existing `if panel.should_close then self:_close_panel(panel) end` (which already generically closes the panel for any panel that set it, no per-flag change needed there).
  Depends on Task 3 (`Item.has_tag`, in case any residual reference is needed — none should remain after step 2's deletion, but confirm no other call site broke), Task 4 (`Customer:show` populating the order panel), and Task 5 (`ItemPanel`'s `should_serve`/`should_skip` flags and `Serve`/`Skip` buttons must exist for this wiring to have anything to react to).

- [x] Task 7 — `tests/test_kitchen_scene.lua` — Update the two tests that currently drive the drag-onto-customer gesture to instead drive click-to-open + Serve/Skip:
  - **Test 1** (currently lines ~46-131, "dragging an item carrying the requested tag onto the waiting customer serves them"): after fast-forwarding until `scene.customer:arrived()`, the customer's greeting (`order_cfg()`'s single message) is still revealing, so `done_talking` is `false`. Click the customer's body (`scene:mouse_pressed(cx, cy)` where `cx, cy = scene.customer.x, scene.customer.y`) to complete the reveal (as today's single-message dialogue click does), click again to flip `done_talking` true, then click a third time to open the order panel (asserting `#scene.panels == 1` and `scene.panels[1].item == scene.customer` afterward — reuse the `_open_or_focus_panel` dedup pattern already exercised by the merchant tests, e.g. Test 6/Test 8). Place the `cooked_meat` item on the floor grid exactly as today, but instead of dragging it onto the customer's body, drag it into the order panel's grid (via `scene.panels[1].item.panel:cell_to_world(0, 0)`, same pattern `microwave.panel:cell_to_world(...)` uses elsewhere in this file) using `scene:mouse_pressed`/`mouse_moved`/`mouse_released` through the panel like Test 4 does for the microwave. Then locate the panel's `Serve` button rect (`scene.panels[1].buttons["Serve"]`) and click its center; assert it is enabled beforehand however you find convenient (e.g. via the panel, or just proceed and check the resulting state), then assert the same post-conditions the old test checked: `scene.day_state.currency == currency_before + 10`, `scene.day_state.customers_served == served_before + 1`, the served item no longer present anywhere (neither the floor grid nor the panel grid), and `#scene.panels == 0` (Serve closes the panel). Keep the rest of Test 1's continuation (the `talking_after`/`advance_after`/next-customer-arrives regression check right after) unchanged — it doesn't touch the drag gesture.
  - **Test 11** (currently lines ~756-804, "dropping the WRONG item on a customer must actually be rejected"): after fast-forwarding to `arrived()` and clicking through the greeting to `done_talking == true` (same sequence as above), click to open the order panel. Drag the wrong item (`raw_meat`, uncooked and taglass, as today) into the panel's grid the same way as Test 1 above. Assert Serve is disabled (e.g. `not scene.panels[1]:_serve_enabled()`, or that clicking Serve's rect does not close the panel / does not change `day_state`). Then click the panel's `Skip` button; assert: the dragged item is back on the main floor grid (not consumed, not stuck in the panel), `scene.day_state.customers_served == served_before + 1` (visit still counts), `scene.day_state.currency == currency_before` (no payment), `scene.customer.dismissed == true`, `#scene.panels == 0` (Skip closes the panel), and — mirroring the old test's rejection-message assertions — that the customer enters `talking_after` with a non-empty `_full_text` (the new skip message), then click through it (`mouse_pressed` twice on the customer body, same completes-reveal/advances-out sequence the old test used) to confirm it still ends in `walking_out`.
  Leave every other test in this file untouched — none of them exercise the drag-onto-customer gesture or the removed `has_tag` local. Depends on Tasks 1-6 all being complete (this task exercises the full stack).

- [x] Task 8 — `tests/test_kitchen_scene.lua`, `tests/test_customer.lua` — Task 7's premise that "every other test in test_kitchen_scene.lua is untouched" was wrong: three more tests exercise the now-deleted drag-onto-customer gesture, and two tests in test_customer.lua assert the old panel-nil-for-non-merchant invariant that Task 4 intentionally changed.
  - `tests/test_kitchen_scene.lua` Test 4 ("dragging an item straight out of an open panel onto the customer serves them"): drags a cooked item straight from the microwave's open panel onto the customer's sprite body. Rewrite to open the order panel first (click through greeting, click to open, same as Task 7's Test 1), then drag the cooked item from the microwave panel into the order panel's grid (a cross-panel drag, same mechanics Test 9 already exercises for dragging between two open panels), then click the order panel's Serve button. Assert the same outcomes as before (currency +10, customers_served +1, item gone from the microwave panel, order panel closes).
  - `tests/test_kitchen_scene.lua` Test 15 ("the broccoli/Cook/steamed_broccoli pipeline"): same shape as Test 4 but for the Healthy tag/steamed_broccoli — apply the identical rewrite pattern.
  - `tests/test_kitchen_scene.lua` Test 16 ("dropping a RAW item directly on a customer is always rejected"): the direct-drop-on-customer-body gesture this test exercises no longer exists at all. Rewrite `assert_raw_drop_rejected` to open the order panel, drag the raw (tagless) item into its grid, assert `_serve_enabled()` is false, click Skip, and assert the item lands back on the floor grid, the visit still counts (`customers_served` +1), no currency awarded, and `customer.dismissed == true` — same shape as Task 7's Test 11 rewrite.
  - `tests/test_customer.lua` Test 5 (~line 246-266, "show() with cfg.kind omitted ... leaves panel/type_id nil"): update the assertions and its own comment/print string — order-kind customers now DO get a panel (3x3, empty) and a `type_id` of `"order_customer"`. Change `assert(c.panel == nil, ...)` to assert `c.panel ~= nil` and `c.panel.cols == config.ORDER_PANEL_COLS` and `c.panel.rows == config.ORDER_PANEL_ROWS` and `#c.panel:items() == 0` (starts empty), and change `assert(c.type_id == nil, ...)` to `assert(c.type_id == "order_customer", ...)`. Rename/reword the test's surrounding comments and PASS message so they no longer claim panel/type_id stay nil for order customers.
  - `tests/test_customer.lua` Test 6 (~line 268-307, "a reused Customer instance correctly resets panel/type_id ... merchant -> order"): same fix — the second `c:show({...})` call (order-kind, no `kind` field) currently asserts `c.panel == nil` and `c.type_id == nil` after switching away from merchant; update those to assert the customer now has a *fresh, empty* 3x3 order panel (`c.panel ~= nil`, `#c.panel:items() == 0`) and `c.type_id == "order_customer"`, not the stale merchant panel/stock. Reword the comments/PASS message accordingly (this is still a real regression guard — just checking "fresh order panel, not leaked merchant stock" instead of "nil").
  - After all edits, run the full test suite (`love . --headless` from `/root/inventory-game`, per Task 7's notes on the actual test runner) and confirm every test file is green, not just these two files.
  Depends on Tasks 1-7 all being complete.
